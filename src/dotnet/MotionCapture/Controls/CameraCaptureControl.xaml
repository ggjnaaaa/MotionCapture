<UserControl x:Class="MotionCapture.Controls.CameraCaptureControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MotionCapture.Controls"
             xmlns:converters="clr-namespace:MotionCapture.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <UserControl.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </UserControl.Resources>
    <UserControl.Background>
        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.MenuBarColorKey}}"/>
    </UserControl.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ComboBox ItemsSource="{Binding AvailableCameras}"
                  SelectedItem="{Binding SelectedCamera}"
                  DisplayMemberPath="Name"
                  Margin="15" Height="30px"
                  Grid.Column="0">
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="IsEnabled"
                    Value="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>

        <Image Source="{Binding RawImage}"
               Stretch="Uniform"
               Grid.Column="1"
               Margin="10"/>

        <Image Source="{Binding SkeletonImage}"
               Stretch="Uniform"
               Grid.Column="2"
               Margin="10"/>
    </Grid>
</UserControl>
