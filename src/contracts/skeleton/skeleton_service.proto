syntax = "proto3";
option csharp_namespace = "MotionCapture.Grpc.Contracts.Skeleton";
package skeleton;

import "skeleton/skeleton_messages.proto";

service SkeletonService {
    rpc StreamSkeleton (SkeletonRequest) returns (stream Skeleton);
}