# Приложение для отслеживания движений человека

## Билд

Установить в python окружение все зависимости (выполнить в папке [python/motion](./src/python/motion/)): ```pip install -r requirements.txt```

Выполнить билд приложения в корне репозитория: ```dotnet build build/build.proj -t:CleanAll,BuildAll```. Эта команда очистит все bin/obj файлы и gRPC контракты в python части и снова соберет dotnet часть и python контракты

## Запуск
1. python/motion часть запускается с команды ``` python -m app.main```
2. dotnet часть запускается либо из .exe в bin у [dotnet/MotionCapture/](./src/dotnet/MotionCapture/). Либо другим удобным способом, например, через Visual Studio

## Ожидаемые обновления
* Добавить в python часть:
    * калибровку камер
    * триангуляцию
    * работу с blender (в т.ч. изучить информацию по взаимодействию blender с python)
* Добавить в dotnet часть:
    * настройки в UI:
        * fps скелета
        * вкл/выкл калибровку
        * общение с blender API
* Желательные:
    * Добавить поддержку bluetooth общения с esp32
    * Сделать уточнение движений за счет датчиков MPU6050
    * Дополнить приложение режимом захвата движений с датчиков (рука)

## Обновления
### 05.03.26

* Добавлен функционал интерактивного отслеживания скелета
* В wpf части приложения можно:
    * выбрать кол-во камер (1 или 2)
    * выбрать камеру
    * увидеть исходный кадр и кадр с обнаруженным скелетом
* в python части:
    * добавлено gRPC API для получения позы с кадра